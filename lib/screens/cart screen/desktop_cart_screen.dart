import 'package:flutter/material.dart';

class DesktopCartScreen extends StatelessWidget {
  const DesktopCartScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
